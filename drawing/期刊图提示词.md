请绘制一张严格对应 `semi_mt_depth_guider_proto_teacher_v1` 策略的高质量论文框架图，任务是腹腔镜手术器械分割。整张图必须明确表现腹腔镜内镜场景中的器械分割，而不是泛化的医学图像分割。输入示例中要能清楚看到细长金属器械、器械尖端、组织背景、高光反射、烟雾、模糊、遮挡以及复杂边界。整体采用白色背景、蓝青绿色科研配色、少量橙色强调损失节点、矢量化、布局均衡、适合医学影像顶刊的精致风格。

整体从左到右布局。左侧显示两组输入：有标注数据包括腹腔镜 RGB 图像、单通道 `depth1c` 深度图、器械真值分割掩码；无标注数据包括腹腔镜 RGB 图像和单通道 `depth1c` 深度图。明确表现 RGB 与深度拼接为 4 通道输入张量，并清楚区分 labeled batch 与 unlabeled batch。分割目标是器械前景与组织背景的分离。

中间主体网络为 `ResNetUNet_DepthGuiderProtoV1` 的 Student 分支。画出五阶段 ResNet 编码器特征 `e1,e2,e3,e4,e5`，以及带跳跃连接的 U-Net 解码器。在每一个编码阶段插入一个 `Depth Guider` 模块。`Depth Guider` 要表现为：输入单通道深度图，经过两层卷积和全局池化提取深度向量，再预测通道级 `gamma` 和 `beta`，然后对当前编码特征做仿射调制并与原特征残差相加。从最深层特征 `e5` 分出一个 `projection head`，结构是 `1x1 conv + batch norm + ReLU + 1x1 conv`，输出原型特征图。解码器输出器械分割 `segmentation logits`。

Student 上方画 `EMA Teacher` 分支，与 Student 架构完全相同，由 Student 克隆得到，只通过 EMA 更新，不参与梯度反传，只接收无标注样本的带噪声输入，输出无标注样本的 `pseudo probability map`。Student 下方画 `Trainable Depth Teacher` 分支，架构也与 Student 相同，同样由 Student 克隆初始化，但它参与梯度优化，接收 RGB 加深度输入和 teacher 风格扰动，输出分割 logits 与伪概率图。图中要明确表现：Student 使用带噪声的全 batch 输入，EMA Teacher 只处理无标注 batch，Depth Teacher 处理带扰动的全 batch。

右侧画出与代码完全一致的优化目标。第一部分是监督损失：对 Student 和 Depth Teacher 的有标注样本分别计算 `cross-entropy` 与 `Dice`，再合成为一个 supervised loss。第二部分是一致性损失：只在 warmup 迭代阈值之后启用，在无标注样本上计算 Student 与 EMA Teacher 之间的概率图均方差一致性，以及 Student 与 Depth Teacher 之间的概率图均方差一致性。第三部分是原型对比损失：同样只在 warmup 阈值之后启用，使用 Student 的原型特征图、下采样到同分辨率的有标注真值掩码、由 EMA 伪标签得到的无标注类别分配图、基于熵分位数上下阈值的有效像素筛选、按类聚合得到的 class anchor、以及每类一个可学习向量组成的原型库，最后通过 `NT-Xent` 进行对比优化。总损失明确表示为 supervised loss + weighted consistency loss + weighted prototype loss。

请增加一个原型学习细节 inset。内容包括：`e5` 投影后的特征图、`L2 normalization`、有标注类别分配、EMA 伪标签类别分配、熵分位数筛选、有效像素掩码、按类平均得到的 `class anchor`、`learnable prototype bank`、以及 class anchor 与 prototype 之间的 `NT-Xent` 匹配关系。箭头规范要明确：实线表示前向推理，虚线表示伪监督，点线表示 EMA update，teacher 输出位置加 stop-gradient 标识。整张图最终要让人一眼看出：这是一个面向腹腔镜手术器械分割的、带深度引导、双教师、半监督、可学习类原型约束的训练框架。

提示词2
请绘制一张腹腔镜手术器械分割的论文级网络结构图，严格对应 `mt_depth_guider_proto_teacher_v1`。输入是腹腔镜 RGB 图像与 `depth1c` 深度图拼接形成的 4 通道张量，输出是器械分割掩码。输入示例必须明显是腹腔镜手术场景，包含金属器械、组织背景、反光、高亮、烟雾、模糊、遮挡以及细长边界。布局为左侧输入、中间三分支网络、右侧损失项。Student 使用 `ResNetUNet_DepthGuiderProtoV1`，包含五层编码器、五个 `Depth Guider`、U-Net 解码器、分割头和从 `e5` 分出的原型投影头。`EMA Teacher` 与 Student 同构，只接收无标注带噪输入，不参与梯度，输出伪概率图。`Trainable Depth Teacher` 与 Student 同构，但可训练，输出 logits 和伪概率图。监督损失是 Student 与 Depth Teacher 在有标注样本上的 `cross-entropy + Dice`。一致性损失是在 warmup 后，对无标注样本计算 Student 与 EMA Teacher、Student 与 Depth Teacher 的 `MSE consistency`。原型对比损失使用 Student 原型特征、有标注 mask、EMA 伪标签、熵分位数筛选、有效像素、按类聚合的 `class anchor`、每类一个可学习 prototype embedding，并通过 `NT-Xent` 优化。整体风格要求白底、蓝青绿色科研配色、少量橙色损失节点、矢量图、高信息密度、简洁专业。

提示词3
请绘制一张双面板学术示意图，对应 `semi_mt_depth_guider_proto_teacher_v1` 腹腔镜手术器械分割策略。Panel A 展示整体训练框架：有标注腹腔镜 RGB-depth1c 输入、无标注腹腔镜 RGB-depth1c 输入、`Student ResNetUNet_DepthGuiderProtoV1`、`EMA Teacher`、`Trainable Depth Teacher`、监督损失、延迟启用的一致性损失、延迟启用的原型对比损失以及 total loss。必须画出五个编码阶段、五个 `Depth Guider`、U-Net 解码器、器械分割 logits、以及从 `e5` 输出的 projection head。明确标出 EMA Teacher 只做 EMA update、不参与梯度；Depth Teacher 参与梯度优化。输入图和输出 mask 要明确体现腹腔镜器械与组织背景的分割，包括细长器械轴、器械尖端、反光、烟雾、模糊和遮挡。Panel B 展示原型学习机制：Student 投影特征图、`L2` 归一化、有标注器械 mask、EMA 伪标签、熵分位数筛选、有效像素掩码、按类聚合的 `class anchor`、可学习类原型向量、以及 `NT-Xent` 对比匹配。重点强调 depth guidance 对腹腔镜器械边界感知的增强，以及 prototype learning 对无标注样本表征对齐的提升。整体风格要求极简、精致、矢量化、白色画布、冷色科研配色、标签清晰、信息密度高。

渲染短标签
有标注图像
无标注图像
腹腔镜RGB图像
Depth1c深度图
器械真值掩码
RGB-Depth1c输入
学生网络
EMA教师
可训练Depth教师
ResNet编码器
U-Net解码器
深度引导模块
Gamma
Beta
残差调制
投影头
原型特征图
分割Logits
伪概率图
可学习原型
类别嵌入库
熵分位数筛选
有效像素掩码
类别锚点
交叉熵损失
Dice损失
MSE一致性
NT-Xent损失
监督损失
一致性损失
原型对比损失
总损失
EMA更新
无梯度
